package test

import (
	"testing"
)

func TestGetChangedData(t *testing.T) {
	//	mngr := newTestChangedDataManager(t)
	//	ctx, cancel := context.WithTimeout(context.Background(), time.Second*20)
	//	defer cancel()
	//	changedData, err := mngr.Get(ctx)
	//	if err != nil {
	//		t.Fatal(err)
	//	}
	//	for _, cd := range changedData {
	//		t.Logf(`=== Changed data ===
	//
	// ID: %d
	// type: %s
	// key: %s`,
	//
	//			cd.ID, cd.DataType.String(), string(cd.Key),
	//		)
	//	}
}
